@model ServiciosWeb.Dominio.Escuela
@{
    ViewBag.Title = "Actualizar";
}

<h2>Actualizar Escuela</h2>
<hr />

<form method="post" action="@Url.Action("Actualizar","School")">
    <div class="form-group">
        <label>IdEscuela</label>
        @Html.TextBoxFor(x => x.Id, new { @class = "form-group", @readonly="readonly" })
    </div>
    <div class="form-group">
        <label>Name</label>
        @Html.TextBoxFor(x => x.Name, new { @class = "form-group" })
    </div>
    <div class="form-group">
        <label>Year</label>
        @Html.TextBoxFor(x => x.Year, new { @class = "form-group" })
    </div>
    <div class="form-group">
        <input type="submit" class="btn btn-success" value="Enviar" />
        <a href="@Url.Action("Index", "School")">Regresar</a>
    </div>
</form>